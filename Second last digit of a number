#include <stdio.h>
int second_last_digit(int a);
int main() {
    printf("%d",second_last_digit(347));
    return 0;
}

int second_last_digit(int a)
{
    if(abs(a)<10)
        return -1;
    else
        return abs((a%100)/10);
}
